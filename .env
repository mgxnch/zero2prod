# This is only used for running `cargo test`
# sqlx reaches out to Postgres at **compile-time** to ensure that queries are well-formed
# It needs this environment variable to ensure that your sqlx::query! macros are well-formed
DATABASE_URL="postgres://postgres:password@localhost:5432/newsletter"